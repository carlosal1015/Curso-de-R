RStudio

En el entorno de arriba derecha se mostrar�n todos los objetos.

Si creamos funciones, listas, dataframes, se guardar�n esa ventana.

En la ventana inferior derecha se ver� la lista de todos los objetos.

Rstudio se conecta con R nativo.

Ctrl + l limpia la pantalla de consola.

Para acceder al historial.

A diferencia de otros programas como Microsoft Word, siempre es posible recuperar en la lista del
historial de comandos de Rstudio.

File es el explorador, no necesariamente es el archivo de trabajo. El plot es un objeto, es un dispositivo
gr�fico fuera del entorno de Rstudio.

Paquetes

Aqu� viene la potencia de R. Se puede instalar nuevas librer�as o paquetes.
R es una comunidad abierta.
R se puede utilizar para cualquier tipo de carreras. Ya supera los 10K de paquetes.
Cada funci�n debe estar documentada y debe tener un ejemplo, ayuda, base de datos.
Todo debe estar documentado.

Cada funci�n tiene ejemplo y tiene su base de datos documentadas.

Hay un mar de informaci�n.

Hay dos formas de cargar librer�as:

Del CRAN o de manera manual.

Si no hay internet se las descarga y luego se carga de manera manual.

R es una comunidad.

Para que funcione correctamente, instale la opci�n habilitada Install dependencies.

Es posible que alguna librer�a ya no est� vigente, es necesario buscar la librer�a que lo reemplaza.

Cuando sea instala no se carga la librer�a.

Instala la librer�a y carga la librer�a.

Es mejor que est� escrito la acci�n y no hacerlo manualmente.

R hace distinci�n entre may�scula y min�scula.

Es importante digitarlo todo en min�scula.

El marco de datos es un tipo especial de objetos.

Se puede usar corchetes o $. Se entiende r�pidamente.

Llamado tipo matricial, espacio (todo)
, separa filas y columnas.

No depende tanto del nombre, sino de la matriz.

Es un factor, al final aparece Levels: X Y.

Factor se parece a un vector, pero puede guardar informaci�n como texto, pero dentro de R es un n�mero.

Hemos visto vector, dataframe y factor (coloca un mensaje en niveles.)

En el objeto factor(1:3, labels=c("A", "B", "C")), el c despu�s de label debe ser en min�scula.

factor es un vector enriquecido o un vector con etiquetas.

factor se utiliza m�s en un dataframe, las columnas, las variables, normalmente van a ser vectores o factores.

Las columnas de un data frame, casi siempre son vectores o factor.

Un data frame est� compuesto de vectores o factores.

La matriz es un arreglo num�rico que puede contener n�meros o texto.

Si contiene texto y n�mero todo es convertido a texto.

El dataframe puede almacenar texto y n�mero sin cambiar sus propiedades, la matriz si lo cambia.

En cambio la matriz cambia sus tipos da datos al insertar texto y n�meros.

En un data frame, debe tener todas la misma cantidad de filas.

En una matriz cuando no coincide el tama�o va a repetir la columna hasta completar, en cambio en el dataframe muestra el mensaje NA.

La carga es por columnas, si se desea cargar por filas se ingresa el siguiente comando:

Con matrix es muy f�cil hacer un llamado, cuando llamamos a un elemento de un marco de datos puede salir como vector o factor, en cambio en el caso de la matriz siempre sale como vector.

En una matriz reescribe los elementos que les falta.

En un data frame no se muestra corchetas, cada fila se le llama casos.

Un data frame nace de las historias m�dicas.

Si no lleva [ e s un data frame.

Cuando se hace el llamado columna por columna lo devuelve exactamente como era.

Si se desea guardar texto y n�mero es preferible usar un data frame, en cambio, si se va a trabajar con n�mero o texto es preferible usar una matriz.

Una lista puede guardar cualquier tipo de objeto.

Incluso puede contener a otra lista.

Una lista usualmente contiene dataframe, matriz, vector, factor, lista.

Las listas se utilizan cuando se usan funciones.

Una lista es el objeto que usa R para expulsar salidas de funciones muy complejas.

Incluso uno de esas salidas puede ser un gr�fica.

El hecho de hacer cambios tiende a modificar los elementos.

Nos gustar�a que no se modifica el objeto en el proceso.

En el ejemplo A es un vector y B es un factor.

Podeos imprimir alg�n elemento del vector con x[[1]][1] por ejemplo.

El c�digo no se altera si le damos ENTER despu�s de una coma ,. Pero no es cierto cuando va entre comillas.

Sirve para analizar series de tiempo.

R est� dirigido a series de tiempos.

Serie de tiempo no es una variable. Se explicar� despu�s.

Se puede aplicar un percentil a una serie de tiempo. Debe existir orden o una mediana.

R es general para muchas disciplinas.

Para la parte matem�tica se puede usar el comando expression().

Para 0 est� reservado el falso, para lo dem�s ser� verdadero.

Si fuera matriz ir�a los corchetes en los encabezados.

El comando mode nos dice de qu� tipo es el objeto vector, por ejemplo numeric or character.

Normalmente cada clase va a ser evaluada, la tercera clase habr�a una evaluaci�n y tambi�n una evaluaci�n final.

Los gr�ficos son sencillos y se deben programar.

Recomendaci�n: Leer el libro.

Cada dispositivo gr�fico lo explica par�metro a par�metro, superposici�n de gr�ficos.

Java de 64 bits, R nativo, borren y actualicen los permisos. Usen de 64 bits.

Codificaci�n al latin1
